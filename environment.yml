name: pointone_take_home
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - pip:
      - fastapi[standard]
      - uvicorn[standard]>=0.32.0
      - psycopg[binary]>=3.2.0
      - pgvector>=0.3.0
      - python-dotenv>=1.0.0
